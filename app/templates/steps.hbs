{{outlet}}

<ul id="step-list">
  <span>
    {{link-to 'SP' 'index' }}
  </span>

  {{#each step in model}}
      <span {{bind-attr class="step.isComplete:completed step.current:current"}}>
      	{{#link-to 'steps.step' step}}{{step.id}}{{/link-to}}
      </span>
  {{/each}}
</ul>