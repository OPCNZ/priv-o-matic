

{{model.id}}

<code>
<p>
We collect personal information from you, including information about your
<ul>
  {{#if model.identityData}}<li>identity</li>{{/if}}
  {{#if model.locationData}}<li>location</li>{{/if}}
  {{#if model.computerNetworkData}}<li>computer/network</li>{{/if}}
  {{#if model.userBehaviourData}}<li>behaviour</li>{{/if}}
  {{#if model.financialData}}<li>finances</li>{{/if}}
  {{#if model.otherDataInfo}}<li>{{model.otherDataInfo}}</li>{{/if}}
</ul>
</p>

<p>
We collect this data from
<ul>
  {{#if model.userDataSource}}<li>the information you give us</li>{{/if}}
  {{#if model.publicDataSource}}<li>public sources</li>{{/if}}
  {{#if model.thirdPartyDataSource}}<li>third parties</li>{{/if}}
  {{#if model.otherDataSourceInfo}}<li>{{model.otherDataSourceInfo}}</li>{{/if}}
</ul>
</p>


<p>
  We collect this data because we need to:
  <ul>
  <li>{{model.collectionReason}}</li>
  {{#each model.extraReasons as |reason|}}
    <li>{{reason}}</li>
  {{/each}}
  </ul>
</p>

<p>
  {{#if model.thirdPartySharingNameOne}}
    Besides our staff, we share this information with {{model.thirdPartySharingNameOne}}
      {{#if model.thirdPartySharingNameTwo}}
        and {{model.thirdPartySharingNameTwo}}
      {{/if}}
    in order to {{model.thirdPartySharingPurpose}}.
  {{/if}}
</p>


<p>
You have the right to ask for a copy of any personal information we hold about you, and to ask for it to be corrected if you think it is wrong. If you’d like to ask for a copy of your information, or to have it corrected, please contact us at {{model.emailAddress}}
  {{#if model.phoneNumber}}(or {{model.phoneNumber}}){{/if}}
  {{#if model.postalAddress}}, or {{model.postalAddress}}){{/if}}.
</p>
</code>


<h2>Data Sources</h2>
<code>
</code>
